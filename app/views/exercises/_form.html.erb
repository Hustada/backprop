<%= form_for([@workout, @workout.exercises.build]) do |f| %>
  <% if @workout.errors.any? %>
	    <div id="errors"
	      <h2><%= pluralize(@workout.errors.count, "error") %> prevented this workout from being saved</h2>
	      <ul>
		    <% @workout.errors.full_messages.each do |msg| %>
		      <li><%= msg %></li>
		      <% end %>
	     </ul>
	    </div>
	<% end %>
 <div class="centering">
 	<%= f.label :name, "Create an exercise" %>
 	<%= f.text_field :name, :placeholder => "squat, dips, bench, etc.." %>

 	<%= f.submit %>
</div> 	
 	
 

<% end %>